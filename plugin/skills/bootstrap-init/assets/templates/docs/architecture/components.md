# Componentes

Componentes internos relevantes por camada/módulo. Se existir um diagrama visual (Mermaid/PlantUML), linke-o aqui em vez de duplicar o conteúdo.

## <Camada 1 — ex.: Camada de entrada/API>

- **<Componente>** — <responsabilidade, onde fica>.

## <Camada 2 — ex.: Regras de negócio>

- **<Componente>** — <responsabilidade, onde fica>.

## <Camada 3 — ex.: Acesso a dados>

- **<Componente>** — <responsabilidade, onde fica>.

## <Camada 4 — ex.: Núcleo/domínio>

- **<Componente>** — <responsabilidade, onde fica>.

## Frontend (se aplicável)

- **<Componente>** — <responsabilidade, onde fica>.

## Observabilidade/infraestrutura transversal

- <Logging, auditoria, rate limiting, correlação de requisições — o que existe e onde>.
