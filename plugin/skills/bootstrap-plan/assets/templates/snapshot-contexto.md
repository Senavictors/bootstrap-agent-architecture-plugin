# Snapshot — <TASK-ID>

Gerado em: <AAAA-MM-DD HH:MM>
Task: `.agents/tasks/backlog/<TASK-ID>.md` (ou `active/`, se já iniciada)

## ADR de referência

<Link para o ADR que motivou esta task — ex.: `.agents/decisions/ADR-004-...md`. Se a task não veio de uma decisão formal, diga isso explicitamente em vez de inventar um link.>

## Assinaturas de código necessárias

<Só o suficiente para retomar sem reabrir o repositório inteiro — não copie o arquivo inteiro, só a assinatura/contrato.>

- `Classe/Função` em `caminho/arquivo.ext` — <o que faz, em 1 linha>
- `Classe/Função` em `caminho/arquivo.ext` — <o que faz, em 1 linha>

## Restrições ativas

<Extraídas de `.agents/decisions/` e `.agents/context/CONTEXT.md` — só as que afetam esta task específica, não a lista inteira do projeto.>

- <restrição 1>

## Próximo passo imediato

<Uma ação única e concreta — não um resumo do que falta fazer inteiro, só o primeiro passo real.>

---
Para retomar: abra uma sessão nova e peça para ler este arquivo antes de continuar a task `<TASK-ID>`.
